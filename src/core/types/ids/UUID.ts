/**
 * A UUID is a unique 128-bit number, stored as 16 octets.
 * UUIDs are parsed as Strings within **GraphQL**.
 * UUIDs are used to assign unique identifiers to entities without requiring a central allocating authority.
 */
type UUID = string

export default UUID
