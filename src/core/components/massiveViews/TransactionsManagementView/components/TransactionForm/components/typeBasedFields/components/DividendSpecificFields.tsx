import React from "react"
import { ITypeBasedFIeldsProps } from "../types"

const DividendSpecificFields = ({
  values,
  errors,
  setFieldError,
  handleChange,
}: ITypeBasedFIeldsProps) => {
  return <div>DividendSpecificFields</div>
}

export default DividendSpecificFields
