import styled from "@emotion/styled";
import { colors } from "src/core/theme";

export const StyledNumberBlockWrapper = styled.div({
  border: `1px solid ${colors.darkGray}`,
  width: 100,
  height: 100,
  borderRadius: 12,
})